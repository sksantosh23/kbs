import { describe, it, expect, beforeEach } from 'vitest';
import { mkdtempSync, rmSync } from 'node:fs'; import { tmpdir } from 'node:os'; import { join } from 'node:path';
import { openDatabase } from '../../src/server/database';
describe('first-party SQLite schema',()=>{let dir:string;beforeEach(()=>{dir=mkdtempSync(join(tmpdir(),'kora-db-'))});it('creates constrained tables and persists inquiry rows',()=>{const db=openDatabase(join(dir,'kora.sqlite')); const now=Date.now(); db.prepare("INSERT INTO inquiries(id,public_reference,type,routing_queue,status,contact_name,email,subject,details,structured_data,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)").run('i1','KORA-TEST','GENERAL','GENERAL','NEW','Test','test@example.test','Topic','Details','{}',now,now); expect((db.prepare('SELECT count(*) n FROM inquiries').get() as any).n).toBe(1); expect((db.prepare("PRAGMA foreign_keys").get() as any).foreign_keys).toBe(1); db.close();rmSync(dir,{recursive:true,force:true})})});
